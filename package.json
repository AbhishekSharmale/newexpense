{
  "name": "expense-tracker",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm run build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "formidable": "^3.5.1"
  }
}